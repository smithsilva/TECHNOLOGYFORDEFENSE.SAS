name: app_t4d
description: Aplicación móvil de Technology for Defense.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  shared_preferences: ^2.5.3

  # Exportación de archivos
  pdf: ^3.10.8
  printing: ^5.14.3
  excel: ^4.0.6
  archive: ^3.4.10
  path_provider: ^2.1.3
  share_plus: ^13.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/imagen10.png
    - assets/escudo1.png